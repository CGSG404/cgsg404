import { Viewport } from 'next'

export const defaultViewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 2,
  minimumScale: 1,
  userScalable: true,
  themeColor: '#000000',
  colorScheme: 'dark'
} 